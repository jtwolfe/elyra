#
# Elyra local configuration (copy to .env and edit)
#
#   cp .env.example .env
#
# IMPORTANT:
# - Do NOT commit .env (it is ignored by .gitignore)
# - Put any private endpoints or keys only in .env (local machine)
#

# LLM backend: "ollama" (default) or "mock"
ELYRA_LLM_BACKEND=ollama

# Ollama model + endpoints
ELYRA_OLLAMA_MODEL=gpt-oss:latest
ELYRA_OLLAMA_BASE_URL_PRIMARY=http://localhost:11434
ELYRA_OLLAMA_BASE_URL_FALLBACK=http://localhost:11434
ELYRA_OLLAMA_TIMEOUT_SECONDS=600
ELYRA_OLLAMA_NUM_CTX=20000

# Persistence
ELYRA_PERSISTENCE_BACKEND=neo4j   # "memory" or "neo4j"
ELYRA_NEO4J_URI=bolt://localhost:7687
ELYRA_NEO4J_USER=neo4j
ELYRA_NEO4J_PASSWORD=password

# Qdrant semantic recall (optional)
ELYRA_ENABLE_QDRANT=1
ELYRA_QDRANT_URL=http://localhost:6333
ELYRA_QDRANT_TOP_K=8

# Web search tool (disabled by default)
ELYRA_ENABLE_WEB_SEARCH=0

# Forking (optional)
ELYRA_ENABLE_FORKING=1

# Dev-only admin endpoints (dangerous)
ELYRA_ENABLE_DANGEROUS_ADMIN=0
